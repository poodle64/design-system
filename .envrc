# Cascade shared config/secrets from the master project's .envrc.local.
source_up

[[ -f .envrc.local ]] && source .envrc.local

# pnpm installs workspace CLI tools (vitest, svelte-check, publint,
# style-dictionary) into node_modules/.bin; put them on PATH for direct use.
PATH_add node_modules/.bin
