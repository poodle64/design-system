{
  "name": "design-system",
  "private": true,
  "type": "module",
  "description": "Household web design-language factory — pnpm workspace: @poodle64/design-tokens + @poodle64/ui.",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "check": "pnpm -r --if-present run check"
  },
  "packageManager": "pnpm@10.34.5"
}
